import { SellerSignupForm } from "@/components/dropshipping/seller-signup-form";

export default function SellPage() {
  return (
    <main style={{ minHeight: "100vh", padding: 40, background: "#f5f5f5" }}>
      <SellerSignupForm />
    </main>
  );
}